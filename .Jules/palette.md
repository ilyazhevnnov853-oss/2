## 2024-05-22 - [Accessibility Essentials]
**Learning:** Icon-only buttons (like back arrows) are invisible to screen readers without `aria-label`, and incorrect `lang` attributes (en vs ru) confuse pronunciation.
**Action:** Always verify `lang` matches content and audit all icon-only buttons for `aria-label` or `aria-labelledby`.
